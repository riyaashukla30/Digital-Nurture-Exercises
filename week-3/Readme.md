This project demonstrates the Adapter Design Pattern in Java.

It integrates multiple payment gateways like PayPal, Stripe, and Razorpay
using a common interface called PaymentProcessor.

Adapters are used to convert different payment gateway methods into a
standard format, making the system flexible and scalable.
